name: Build

run-name: Build for ${{ github.ref }}

on:
  push:
    branches:
      - main
      - 'release/*'
  workflow_call:
    outputs:
      release_path:
        description: Folder in which the application was published to.
        value: $ {{ env.PUBLISH_FOLDER }}

env:
  PUBLISH_FOLDER: release

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
          cache-dependency-path: '**/packages.lock.json'
          cache: true

      - name: Restore Dependencies
        run: dotnet restore --locked-mode

      - name: Build Solution
        run: dotnet publish -c Release -o $PUBLISH_FOLDER CleanShortener.sln --no-restore

      - name: Publish Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: application
          path: $PUBLISH_FOLDER/